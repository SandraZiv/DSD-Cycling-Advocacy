# TODO road_analysis.py
import logging
import time

logging.basicConfig(filename='log.log', level=logging.DEBUG)


# TODO trip analysis
def run_trip_analysis(trip_id):
    logging.info('Performing trip analysis of trip: %s' % trip_id)
    time.sleep(0.1)
    return


# TODO build_map
def build_map():
    pass


# TODO update_map
def update_map(trip_id):
    logging.info('Performing map updating with trip: %s' % trip_id)
    time.sleep(0.1)
    return
